#include <bits/stdc++.h>
using namespace std;
#define ALL(a) (a).begin(), (a).end()
const int INF = 1e9;
const int MOD = 1e9 + 7;
const long long LINF = 1e18;

int main(){

  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  // input
  vector<int> f(100);
  for (auto& x: f) cin >> x;

  // solve
  for (int i=0; i<100; i++){
    int pt;
    cin >> pt;
    cout << "R" << endl;
  }

  return 0;
  
}